export enum CharacterState {
    Idle = 0,
    Walk = 1,
    Attack = 2,
    Hurt = 3,
    Death = 4
}

export enum CharacterName {
    Death = "death",
    Demon = "demon",
    Skeleton = "skeleton"
}

export enum CharacterSide {
    Left = 0,
    Right = 1
}